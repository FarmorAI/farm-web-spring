<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.farmorai.backend.mapper.BoardMapper">
    <!-- 페이징 처리된 게시글 리스트 조회 -->
    <select id="getBoardList" parameterType="com.farmorai.backend.dto.PageRequestDto" resultType="com.farmorai.backend.dto.BoardDto">
        SELECT b.board_id, b.title, m.nickname writer, b.content, b.views, b.created_at, b.updated_at
        FROM board b
        inner join member m on b.member_id = m.member_id
        <where>
            1=1
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="content != null and content != ''">
                AND content LIKE CONCAT('%', #{content}, '%')
            </if>
        </where>
        ORDER BY board_id DESC
            LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 전체 게시글 개수 조회 -->
    <select id="getBoardListCount" parameterType="Map" resultType="int">
        SELECT COUNT(*) FROM board
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="content != null and content != ''">
                AND content LIKE CONCAT('%', #{content}, '%')
            </if>
        </where>
    </select>

    <select id = "getBoardDetail" parameterType="Long" resultType="com.farmorai.backend.dto.BoardDto">
        SELECT b.board_id, b.title, m.nickname writer, b.content, b.views, b.created_at, b.updated_at
        FROM board b
        inner join member m on b.member_id = m.member_id
        WHERE board_id = #{boardId}
    </select>

    <insert id = "insertBoard" parameterType="com.farmorai.backend.dto.BoardDto">
        INSERT INTO board (title, content, member_id, created_at, updated_at) VALUES (#{board.title}, #{board.content}, #{memberId},now(), now())
    </insert>

    <delete id = "deleteBoard" parameterType="Long">
        DELETE FROM board WHERE board_id = #{boardId}
    </delete>

    <update id = "updateBoard" parameterType = "com.farmorai.backend.dto.BoardDto">
        UPDATE board SET title = #{board.title}, content = #{board.content}, updated_at = now() WHERE board_id = #{boardId}
    </update>

    <update id = "updateBoardViews" parameterType = "Long">
        UPDATE board SET views = views + 1 WHERE board_id = #{boardId}
    </update>
</mapper>