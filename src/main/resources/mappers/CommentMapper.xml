<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.farmorai.backend.mapper.CommentMapper">

    <!-- 댓글 목록 조회 (대댓글 포함, 정렬 적용) -->
    <select id="getCommentList" parameterType="Long" resultType="com.farmorai.backend.dto.CommentDto">
        SELECT * FROM comment
        WHERE board_id = #{boardId}
        ORDER BY ref ASC, level ASC
    </select>

    <!-- 특정 댓글 조회 -->
    <select id="getCommentById" parameterType="long" resultType="com.farmorai.backend.dto.CommentDto">
        SELECT * FROM comment WHERE comment_id = #{commentId}
    </select>

    <!-- 댓글 삽입 -->
    <insert id="insertComment" parameterType="com.farmorai.backend.dto.CommentDto" useGeneratedKeys="true" keyProperty="commentId">
        INSERT INTO comment (board_id, member_id, content, created_at, updated_at, ref, depth, level, parent_id)
        VALUES (#{boardId}, #{memberId}, #{content}, now(), now(), #{ref}, #{depth}, #{level}, #{parentId})
    </insert>

    <!-- 새 댓글이 원 댓글인 경우 ref 값을 자신의 comment_id로 업데이트 -->
    <update id="updateCommentRef">
        UPDATE comment SET ref = #{commentId} WHERE comment_id = #{commentId} AND ref = 0
    </update>

    <!-- 대댓글 존재 여부 확인 -->
    <select id="hasChildComments" parameterType="long" resultType="boolean">
        SELECT EXISTS (
            SELECT 1 FROM comment WHERE ref = #{commentId} AND depth > 0
        )
    </select>

    <!-- 댓글 소프트 삭제 -->
    <update id="markCommentAsDeleted">
        UPDATE comment SET content = '[삭제된 댓글입니다.]' WHERE comment_id = #{commentId}
    </update>

    <!-- 댓글 삭제 (대댓글이 없을 경우만) -->
    <delete id="deleteComment" parameterType="Long">
        DELETE FROM comment WHERE comment_id = #{commentId} AND NOT EXISTS (
            SELECT 1 FROM comment WHERE ref = #{commentId}
        )
    </delete>

    <!-- 댓글 내용 수정 -->
    <update id="updateComment" parameterType="com.farmorai.backend.dto.CommentDto">
        UPDATE comment SET content = #{content}, updated_at = now() WHERE comment_id = #{commentId}
    </update>

    <!-- 특정 ref 그룹 내에서 가장 깊은 depth 조회 -->
    <select id="getMaxDepthByRef" parameterType="long" resultType="int">
        SELECT COALESCE(MAX(depth), 0) FROM comment WHERE ref = #{ref}
    </select>

</mapper>
